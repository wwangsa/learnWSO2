openapi: 3.0.1
info:
  title: EcommerceAPI
  description: API Definition of EcommerceAPI
  version: 1.0.0
servers:
- url: http://localhost:8290/orders
paths:
  /:
    get:
      responses:
        default:
          description: Default response
  /month/{currentMonth}:
    get:
      parameters:
      - name: currentMonth
        in: path
        required: true
        style: simple
        explode: false
        schema:
          type: string
      responses:
        default:
          description: Default response
  /list:
    get:
      responses:
        default:
          description: Default response
